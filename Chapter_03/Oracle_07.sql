CREATE TABLE TAB_PADRAO
(ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
DESCRITOR VARCHAR(10) NOT NULL,
ENDERECO VARCHAR(100) NULL,
CIDADE VARCHAR(50) DEFAULT ON NULL 'Rio de Janeiro',
DATA_CRIACAO DATE DEFAULT ON NULL SYSDATE,
PRIMARY KEY(ID));

-- If insert data without the description of the columns, with only some columns,
-- the oracle will present a error: not enough values
INSERT INTO TAB_PADRAO (DESCRITOR, ENDERECO, CIDADE, DATA_CRIACAO) VALUES 
('CLIENTE X', 'RUA PROJETADA A', 'SAO PAULO', TO_DATE('2019-01-01','YYYY-MM-DD'));

INSERT INTO TAB_PADRAO(DESCRITOR) VALUES ('CLIENTE Y');

SELECT * FROM TAB_PADRAO;
